<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kni.mobile.kalendarzAkademickiPK"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="kni.mobile.kalendarzAkademickiPK.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="kni.mobile.kalendarzAkademickiPK.CalendarMenu" android:label="CalendarMenu"></activity>
        <activity android:name="kni.mobile.kalendarzAkademickiPK.CalendarInsertSched" android:label="CalendarInsertSched"></activity>
        <activity android:name="kni.mobile.kalendarzAkademickiPK.Author"></activity>
        <activity android:label="Forum" android:name="kni.mobile.kalendarzAkademickiPK.WebviewActivity"></activity>
    </application>

</manifest>